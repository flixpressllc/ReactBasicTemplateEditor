import * as getAudioOptions from './getAudioOptions';

describe('getAutioOptions', () => {
  describe('custom audio', () => {
    it('returns an empty array if custom audio is unavailable', () => {
      pending();
    });
  });
});
